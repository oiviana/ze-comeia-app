import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export function NotLoggedAccount(){
    return (
        <SafeAreaView>
            <Text>Nao logado</Text>
        </SafeAreaView>
    )
}